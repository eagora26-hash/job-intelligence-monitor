# Development & CI tooling. Install on top of the dashboard + API layers
# (so the full test suite — including the REST API tests — runs in CI).
-r requirements-dashboard.txt
-r requirements-api.txt
pytest>=8.0
pytest-cov>=4.1
ruff>=0.6
